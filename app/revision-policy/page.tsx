import LegalPage from '../legal-page';
import { resolveLocale } from '../locale';

export const dynamic = 'force-dynamic';
export default async function RevisionPolicyPage({ searchParams }: { searchParams?: Promise<{ lang?: string | string[] }> }) { const query = await searchParams; const locale = await resolveLocale(query?.lang); return <LegalPage locale={locale} backLabel="Back to Elx Studio" title="Revision Policy" intro="Revisions are intended to bring the delivered work into alignment with the approved brief. They do not automatically expand the original project scope." sections={[{ heading: 'Included revisions', body: 'The quote should state the number of included revision rounds and the review window. Revision notes should be consolidated, specific and connected to the approved instructions.' }, { heading: 'Out-of-scope requests', body: 'New deliverables, changed direction, missing source information or requirements introduced after approval may need a new quote and deadline.' }, { heading: 'Revision window', body: 'Clients should review delivered work promptly and submit revision notes within the period stated in the quote or delivery message.' }, { heading: 'Final approval', body: 'An order may be treated as completed after written approval, expiration of the revision window or delivery of the agreed revision rounds.' }]} />; }
