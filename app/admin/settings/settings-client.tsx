'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
export type SettingRow = { key: string; value: unknown; updated_at: string };
export default function SettingsClient({ settings, canManage }: { settings: SettingRow[]; canManage: boolean }) { const router = useRouter(); const [drafts, setDrafts] = useState<Record<string, string>>(Object.fromEntries(settings.map((item) => [item.key, JSON.stringify(item.value, null, 2)]))); const [busy, setBusy] = useState(''); const [error, setError] = useState(''); async function save(key: string) { setBusy(key); setError(''); let value: unknown; try { value = JSON.parse(drafts[key]); } catch { setError(`${key}: invalid JSON.`); setBusy(''); return; } const response = await fetch(`/api/admin/settings/${encodeURIComponent(key)}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ value }) }); const body = await response.json().catch(() => ({})) as { error?: string }; if (!response.ok) setError(body.error || 'Setting could not be saved.'); else router.refresh(); setBusy(''); }
  return <div className="grid gap-6"><section className="bg-white p-7 md:p-9"><p className="text-xs font-black uppercase tracking-[.14em] text-[#F06449]">Configuration</p><h1 className="mt-2 text-5xl font-black tracking-[-.06em]">Workflow settings.</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-black/50">Only non-secret operational JSON is editable here. Credentials belong in Vercel environment variables and never in this database screen.</p></section>{!canManage && <p className="bg-[#FFF0E8] p-4 text-sm font-bold">Read-only: administrator role is required to save settings.</p>}<section className="grid gap-5 xl:grid-cols-2">{settings.map((setting) => <article key={setting.key} className="bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-black">{setting.key.replaceAll('_', ' ')}</h2><span className="text-[10px] text-black/35">{new Date(setting.updated_at).toLocaleString()}</span></div><textarea className="mt-5 min-h-64 w-full bg-[#102321] p-5 font-mono text-xs leading-5 text-white/70 outline-none" value={drafts[setting.key] || ''} onChange={(event) => setDrafts({ ...drafts, [setting.key]: event.target.value })} spellCheck={false} /><button onClick={() => void save(setting.key)} disabled={!canManage || busy === setting.key} className="mt-4 bg-[#102321] px-5 py-3 text-xs font-black text-white disabled:opacity-35">Save setting</button></article>)}{settings.length === 0 && <p className="bg-white p-6 text-black/45">No non-secret settings found.</p>}</section>{error && <p className="bg-red-50 p-4 text-sm font-bold text-red-700">{error}</p>}</div>; }
