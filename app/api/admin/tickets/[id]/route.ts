import { NextResponse } from 'next/server';
import { logAdminAudit } from '../../../../lib/admin-audit';
import { getAuthContext, staffRoles } from '../../../../lib/auth';
import { getSupabaseAdminClient } from '../../../../lib/supabase/admin';
const validStatuses = new Set(['open', 'in_progress', 'waiting_on_client', 'resolved', 'closed', 'reopened']);
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { const { user, roles } = await getAuthContext(); if (!user) return NextResponse.json({ error: 'Sign in required.' }, { status: 401 }); if (!roles.some((role) => staffRoles.includes(role as (typeof staffRoles)[number]))) return NextResponse.json({ error: 'Staff access required.' }, { status: 403 }); const { id } = await params; const body = await request.json().catch(() => ({})) as { status?: string; assignedTo?: string | null }; const admin = getSupabaseAdminClient(); if (!admin) return NextResponse.json({ error: 'Database unavailable.' }, { status: 503 }); const { data: old } = await admin.from('support_tickets').select('status, assigned_to').eq('id', id).maybeSingle(); if (!old) return NextResponse.json({ error: 'Ticket not found.' }, { status: 404 }); const updates: Record<string, string | null> = {}; if (body.status !== undefined) { if (!validStatuses.has(body.status)) return NextResponse.json({ error: 'Invalid ticket status.' }, { status: 400 }); updates.status = body.status; if (body.status === 'closed') updates.closed_at = new Date().toISOString(); } if (body.assignedTo !== undefined) { if (body.assignedTo !== null && !/^[0-9a-f-]{36}$/i.test(body.assignedTo)) return NextResponse.json({ error: 'Invalid assignee.' }, { status: 400 }); updates.assigned_to = body.assignedTo; } if (!Object.keys(updates).length) return NextResponse.json({ error: 'No supported changes.' }, { status: 400 }); const { error } = await admin.from('support_tickets').update(updates).eq('id', id); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); await logAdminAudit({ actorId: user.id, action: 'ticket.updated', entityType: 'support_ticket', entityId: id, oldData: old, newData: updates, request }); return NextResponse.json({ ok: true }); }
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const { user, roles } = await getAuthContext(); if (!user) return NextResponse.json({ error: 'Sign in required.' }, { status: 401 }); if (!roles.some((role) => staffRoles.includes(role as (typeof staffRoles)[number]))) return NextResponse.json({ error: 'Staff access required.' }, { status: 403 }); const { id } = await params; const body = await request.json().catch(() => ({})) as { body?: string }; const message = String(body.body || '').trim().slice(0, 5000); if (!message) return NextResponse.json({ error: 'Reply is required.' }, { status: 400 }); const admin = getSupabaseAdminClient(); if (!admin) return NextResponse.json({ error: 'Database unavailable.' }, { status: 503 }); const { data: ticket } = await admin.from('support_tickets').select('created_by, ticket_number').eq('id', id).maybeSingle(); if (!ticket) return NextResponse.json({ error: 'Ticket not found.' }, { status: 404 }); const { error } = await admin.from('ticket_messages').insert({ ticket_id: id, sender_id: user.id, body: message }); if (error) return NextResponse.json({ error: error.message }, { status: 400 }); await admin.from('notifications').insert({ user_id: ticket.created_by, event_type: 'support_reply', title: `Reply on ${ticket.ticket_number}`, body: message.slice(0, 180), data: { ticket_id: id } }); await logAdminAudit({ actorId: user.id, action: 'ticket.replied', entityType: 'support_ticket', entityId: id, newData: { body_length: message.length }, request }); return NextResponse.json({ ok: true }); }
